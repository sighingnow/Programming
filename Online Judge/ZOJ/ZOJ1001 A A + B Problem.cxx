#include <iostream>
#include <cstdio>
using namespace std;

//ZOJ 1001

int main(int argc, char **argv)
{
    int x, y;
    while(scanf("%d %d", &x, &y) == 2)
    {
        printf("%d\n", x + y);
    }
    return 0;
}

